#include <iostream>
#include "engine/engine.h"

int main(int argc, char *argv[]) {

    std::cout << "Hollow world" << std::endl;

    Engine engine;
    engine.Run();
    // if (engine != nullptr) {

    // }

    return 0;
}
